<Window x:Class="whisperMeOff.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:whisperMeOff"
        mc:Ignorable="d"
        Title="whisperMeOff - Local AI Chat" Height="650" Width="950"
        Background="#FAFAFA"
        WindowStartupLocation="CenterScreen">

    <Window.InputBindings>
        <KeyBinding Key="Add" Modifiers="Control" Command="{x:Static local:MainWindow.ZoomInCommand}"/>
        <KeyBinding Key="OemPlus" Modifiers="Control" Command="{x:Static local:MainWindow.ZoomInCommand}"/>
        <KeyBinding Key="Subtract" Modifiers="Control" Command="{x:Static local:MainWindow.ZoomOutCommand}"/>
        <KeyBinding Key="OemMinus" Modifiers="Control" Command="{x:Static local:MainWindow.ZoomOutCommand}"/>
    </Window.InputBindings>

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078D4"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Chat Bubble User Style -->
        <SolidColorBrush x:Key="UserBubble" Color="#0078D4"/>
        <SolidColorBrush x:Key="AIBubble" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="UserText" Color="White"/>
        <SolidColorBrush x:Key="AIText" Color="#333333"/>
    </Window.Resources>

    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform x:Name="WindowScale" ScaleX="1" ScaleY="1"/>
        </Grid.LayoutTransform>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Modern Menu -->
        <Menu Grid.Row="0" Background="White" Padding="4,2">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="🤖 _File" FontSize="12">
                <MenuItem Header="_Zoom In" Click="ZoomIn_Click" InputGestureText="Ctrl++"/>
                <MenuItem Header="Zoom _Out" Click="ZoomOut_Click" InputGestureText="Ctrl+-"/>
                <Separator/>
                <MenuItem Header="_Settings" Click="Settings_Click"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_Help" FontSize="12">
                <MenuItem Header="ℹ️ _About" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <!-- Chat Messages Display -->
        <Border Grid.Row="1" Background="#FAFAFA" Padding="10,10">
            <ListBox x:Name="ChatMessages" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     BorderThickness="0" Padding="30,15"
                     Background="Transparent">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="{Binding HorizontalAlignment}">
                            <Border Background="{Binding BackgroundColor}" 
                                    CornerRadius="16" Padding="16,12" Margin="0"
                                    MaxWidth="550">
                                <StackPanel>
                                    <TextBlock Text="{Binding Sender}" 
                                               FontSize="12"
                                               FontWeight="SemiBold" 
                                               Foreground="{Binding SenderColor}"
                                               Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Message, Mode=OneWay}" 
                                             TextWrapping="Wrap"
                                             FontSize="14"
                                             Foreground="{Binding TextColor}"
                                             IsReadOnly="True"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Cursor="Hand"
                                             SelectionBrush="#B4D7FF"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Input Area -->
        <Border Grid.Row="2" Background="White" Padding="20" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="MessageInput" 
                         Grid.Column="0"
                         Style="{StaticResource ModernTextBox}"
                         Height="50" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalContentAlignment="Center"
                         PreviewKeyDown="MessageInput_PreviewKeyDown"/>
                <Button x:Name="MicButton" 
                        Grid.Column="1" 
                        Style="{StaticResource ModernButton}"
                        Content="🎤" 
                        Margin="12,0,0,0"
                        Click="MicButton_Click"
                        ToolTip="Click to start voice input"/>
                <Button x:Name="SendButton" 
                        Grid.Column="2" 
                        Style="{StaticResource ModernButton}"
                        Content="Send" 
                        Margin="12,0,0,0"
                        Click="Send_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
